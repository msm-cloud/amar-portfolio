# admin/

Admin dashboard route(s). Will be auth-protected in a later step (via
Supabase auth + middleware/route handlers). Empty for now — this folder
exists to reserve the convention before auth is wired up.
