# api/

Next.js Route Handlers (`route.ts`), if/when the app needs a plain HTTP
endpoint instead of a Server Action (e.g. webhooks, third-party callbacks).
Empty for now — prefer Server Actions (`src/server/actions/`) for regular
form/data mutations.
