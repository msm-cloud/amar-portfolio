# components/sections/

Page-level sections composed from `components/ui/` primitives: `Hero`,
`About`, `Projects`, `Contact`, etc. These are specific to a page/route
and generally not reused outside of it.
